<Page
    x:Class="PoolController.StatusView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PoolController"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ctrls="using:PoolController.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer IsTabStop="True">
            <Grid Margin="20,0"
      VerticalAlignment="Center" ColumnSpacing="20">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320 "/>
                <ColumnDefinition  Width="*" />
            </Grid.ColumnDefinitions>
            <ctrls:PumpStatus />


            <StackPanel Grid.Column="1">
            <Border Grid.Column="1"
        Padding="20" 
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        CornerRadius="16"
        Background="White"
        BorderBrush="#2A2F36"
        BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="Temperature" FontSize="16" FontWeight="SemiBold"/>

                    <TextBlock  Grid.Row="1" Text="Pool" />
                    <TextBlock  Grid.Row="1" Text="58.4 °F" Grid.Column="1" HorizontalAlignment="Right" FontWeight="Bold" />
                    <TextBlock  Grid.Row="2" Text="Return"  />
                    <TextBlock  Grid.Row="2" Text="64.2 °F" Grid.Column="1" HorizontalAlignment="Right" FontWeight="Bold" />
                    <TextBlock  Grid.Row="3" Text="Air" />
                    <TextBlock  Grid.Row="3" Text="68.8 °F" Grid.Column="1" HorizontalAlignment="Right" FontWeight="Bold" />
                    <TextBlock  Grid.Row="4" Text="Gas Heater" />
                    <TextBlock  Grid.Row="4" Text="Disabled" Grid.Column="1" HorizontalAlignment="Right" FontWeight="Bold" />
                    <TextBlock  Grid.Row="5" Text="Solar Heater" />
                    <TextBlock  Grid.Row="5" Text="Enabled" Grid.Column="1" HorizontalAlignment="Right" FontWeight="Bold" />
                </Grid>
            </Border>
            </StackPanel>
            <!--<TextBlock Text="Pump Power (W):" FontWeight="Bold" Margin="0,20,0,0" />
                <TextBlock Text="{x:Bind Service.PumpStatus.Power, Mode=OneWay}" />
                <TextBlock Text="Pump Speed (RPM):" FontWeight="Bold" Margin="0,20,0,0" />
                <TextBlock Text="{x:Bind Service.PumpStatus.PumpSpeed, Mode=OneWay}" />
                <TextBlock Text="Flow (GPM):" FontWeight="Bold" Margin="0,20,0,0" />
                <TextBlock Text="{x:Bind Service.PumpStatus.EstimatedFlow, Mode=OneWay}" />
                <TextBlock Text="Pump Clock:" FontWeight="Bold" Margin="0,20,0,0" />
                <TextBlock Text="{x:Bind Service.PumpStatus.Clock, Mode=OneWay}" />
                <TextBlock Text="Run State:" FontWeight="Bold" Margin="0,20,0,0" />
                <TextBlock Text="{x:Bind Service.PumpStatus.Running, Mode=OneWay}" />
                <TextBlock Text="Status:" FontWeight="Bold" Margin="0,20,0,0" />
                <TextBlock Text="{x:Bind Service.PumpStatus.State, Mode=OneWay}" />-->
            </Grid>
    </ScrollViewer>
</Page>
