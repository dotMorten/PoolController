<UserControl
    x:Class="PoolController.Controls.SemiRingGauge"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PoolController.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <!-- Track -->
        <Path Stroke="{x:Bind TrackBrush}" x:Name="TrackPath"
              StrokeThickness="{x:Bind StrokeThickness}"
              StrokeStartLineCap="Round"
              StrokeEndLineCap="Round"/>

        <!-- Value -->
        <Path Stroke="{x:Bind ValueBrush}" x:Name="ValuePath"
              StrokeThickness="{x:Bind StrokeThickness}"
              StrokeStartLineCap="Round"
              StrokeEndLineCap="Round"/>
    </Grid>
</UserControl>
