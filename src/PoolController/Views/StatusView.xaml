<Page
    x:Class="PoolController.Views.StatusView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PoolController"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ctrls="using:PoolController.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="6" VerticalAlignment="Center" ColumnSpacing="6" RowSpacing="6">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition  Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Style="{StaticResource GridPanel}" Grid.RowSpan="2">
            <ctrls:PumpStatus VerticalAlignment="Stretch" />
        </Grid>

        <Grid Grid.Column="1" Style="{StaticResource GridPanel}">
            <ctrls:ChlorinatorStatus />
        </Grid>
        
        <Grid Style="{StaticResource GridPanel}" Grid.Column="1" Grid.Row="2">
            <StackPanel Spacing="6" >
                <Grid Grid.Row="0" >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Spacing="2">
                        <TextBlock Text="Heating" FontSize="16" FontWeight="SemiBold"/>
                    </StackPanel>
                    <StackPanel Spacing="6">
                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Top" HorizontalAlignment="Right">
                        <TextBlock Text="Solar" Opacity="0.75"/>
                        <ToggleButton IsChecked="{x:Bind Service.IsSolarHeating, Mode=OneWay}" >
                            <ToggleButton.Template>
                                <ControlTemplate>
                                    <Grid>
                                        <VisualStateManager.VisualStateGroups>
                                            <VisualStateGroup x:Name="CommonStates">
                                                <VisualState x:Name="Checked">
                                                    <VisualState.Setters>
                                                        <Setter Target="On.Visibility" Value="Visible" />
                                                        <Setter Target="Off.Visibility" Value="Collapsed" />
                                                    </VisualState.Setters>
                                                </VisualState>
                                                <VisualState x:Name="Normal">
                                                    <VisualState.Setters>
                                                        <Setter Target="On.Visibility" Value="Collapsed" />
                                                        <Setter Target="Off.Visibility" Value="Visible" />
                                                    </VisualState.Setters>
                                                </VisualState>
                                            </VisualStateGroup>
                                        </VisualStateManager.VisualStateGroups>
                                        <StackPanel Orientation="Horizontal" x:Name="On" Spacing="4" Visibility="Collapsed" >
                                            <TextBlock Text="On" />
                                            <Ellipse Fill="Green" Width="8" Height="8"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" x:Name="Off" Spacing="4">
                                            <TextBlock Text="Off" />
                                            <Ellipse Fill="Red" Width="8" Height="8"/>
                                        </StackPanel>                                        
                                    </Grid>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                    </StackPanel>


                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Top" HorizontalAlignment="Right">
                        <TextBlock Text="Gas" Opacity="0.75"/>
                        <ToggleButton IsChecked="False" >
                            <ToggleButton.Template>
                                <ControlTemplate>
                                    <Grid>
                                        <VisualStateManager.VisualStateGroups>
                                            <VisualStateGroup x:Name="CommonStates">
                                                <VisualState x:Name="Checked">
                                                    <VisualState.Setters>
                                                        <Setter Target="On.Visibility" Value="Visible" />
                                                        <Setter Target="Off.Visibility" Value="Collapsed" />
                                                    </VisualState.Setters>
                                                </VisualState>
                                                <VisualState x:Name="Normal">
                                                    <VisualState.Setters>
                                                        <Setter Target="On.Visibility" Value="Collapsed" />
                                                        <Setter Target="Off.Visibility" Value="Visible" />
                                                    </VisualState.Setters>
                                                </VisualState>
                                            </VisualStateGroup>
                                        </VisualStateManager.VisualStateGroups>
                                        <StackPanel Orientation="Horizontal" x:Name="On" Spacing="4" Visibility="Collapsed" >
                                            <TextBlock Text="On" />
                                            <Ellipse Fill="Green" Width="8" Height="8"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" x:Name="Off" Spacing="4">
                                            <TextBlock Text="Off" />
                                            <Ellipse Fill="Red" Width="8" Height="8"/>
                                        </StackPanel>                                        
                                    </Grid>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                    </StackPanel>
                    </StackPanel>
                </Grid>
                <TextBlock Text="Heating to 85F" Visibility="{x:Bind Service.IsSolarHeating, Mode=OneWay}" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
