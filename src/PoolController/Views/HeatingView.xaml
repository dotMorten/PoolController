<Page
    x:Class="PoolController.Views.HeatingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PoolController.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="6" ColumnSpacing="6">
        <Grid.Resources>
            <x:Double x:Key="SettingsCardWrapThreshold">800</x:Double>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid VerticalAlignment="Stretch" BorderBrush="Gray" Background="White" CornerRadius="4" Padding="16" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
                <FontIcon Glyph="&#xE706;" HorizontalAlignment="Center" VerticalAlignment="Top" />
                <StackPanel Grid.Column="1" Spacing="8">
                    <TextBlock Text="Solar Heating" />
                <Grid>
                    <Slider Header="Thermostat" x:Name="ThermostatSlider"
                            Minimum="50" Maximum="100" Value="85" HorizontalAlignment="Stretch"/>
                    <TextBlock HorizontalAlignment="Right" VerticalAlignment="Top" Text="{x:Bind ThermostatSlider.Value.ToString('0°F'), Mode=OneWay}"></TextBlock>
                </Grid>

                <ComboBox Header="Mode" SelectedIndex="0" HorizontalAlignment="Stretch">
                    <ComboBoxItem>Auto</ComboBoxItem>
                    <ComboBoxItem>On</ComboBoxItem>
                    <ComboBoxItem>Off</ComboBoxItem>
                </ComboBox>
            </StackPanel>
        </Grid>

        <Grid Grid.Column="1" VerticalAlignment="Stretch" BorderBrush="Gray" Background="White" CornerRadius="4" Padding="16" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <FontIcon Glyph="&#xECAD;" HorizontalAlignment="Center" VerticalAlignment="Top" />
            <StackPanel Grid.Column="1" Spacing="8">
                <TextBlock Text="Gas Heating" />
                <Grid>
                    <Slider Header="Thermostat" x:Name="GasThermostatSlider"
                            Minimum="50" Maximum="100" Value="90"  HorizontalAlignment="Stretch"/>
                    <TextBlock HorizontalAlignment="Right" VerticalAlignment="Top" Text="{x:Bind GasThermostatSlider.Value.ToString('0°F'), Mode=OneWay}"></TextBlock>
                </Grid>

                <ComboBox Header="Mode" SelectedIndex="0" HorizontalAlignment="Stretch">
                    <ComboBoxItem>Off</ComboBoxItem>
                    <ComboBoxItem>On</ComboBoxItem>
                </ComboBox>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
